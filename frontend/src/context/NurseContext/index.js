// Export all contexts from the Nurse module
import { InventoryContext, InventoryProvider, useInventory } from './InventoryContext';

export {
  // Inventory context exports
  InventoryContext,
  InventoryProvider,
  useInventory,
  
  // Add other contexts as they are created
};
